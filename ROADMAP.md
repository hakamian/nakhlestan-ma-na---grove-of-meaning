
# نقشه راه فنی نخلستان معنا (مسیر تبدیل MVP به Production)

این سند وضعیت فعلی پروژه و گام‌های ضروری برای لانچ موفق برای ۱۰۰۰ کاربر و بیشتر را تشریح می‌کند.

## 🚨 وضعیت فعلی: نسخه نمایشی (Prototype)
- **نوع معماری:** Client-Side Only (SPA)
- **ذخیره‌سازی داده:** LocalStorage (ناپایدار، وابسته به دستگاه کاربر)
- **امنیت:** پایین (کلیدهای API در فرانت‌اند در دسترس هستند)
- **وضعیت:** مناسب برای ارائه به سرمایه‌گذار / تست UX. **نامناسب برای لانچ عمومی.**

---

## 🛠 گام‌های حیاتی برای لانچ (Priority 1)
*بدون انجام این موارد، لانچ نکنید.*

### ۱. مهاجرت دیتابیس (Database Migration)
- **اقدام:** جایگزینی `dbAdapter.ts` فعلی با کلاینت واقعی Supabase یا Firebase.
- **هدف:** حفظ اطلاعات کاربر در صورت تعویض دستگاه یا پاک کردن کش مرورگر.
- **ساختار پیشنهادی:**
  - جدول `users` (پروفایل، امتیازات)
  - جدول `orders` (سفارشات، وضعیت پرداخت)
  - جدول `timeline_events` (رویدادهای گاهشمار)

### ۲. ایمن‌سازی هوش مصنوعی (AI Security Proxy)
- **مشکل:** کلید Gemini API در حال حاضر در کلاینت است.
- **اقدام:** ایجاد یک API Route (مثلاً در Vercel Functions یا Cloudflare Workers).
- **جریان:** `Client` -> `Server (Your Key)` -> `Google Gemini`.

### ۳. درگاه پرداخت واقعی
- **اقدام:** اتصال به زرین‌پال یا درگاه‌های واسط.
- **نکته:** تایید تراکنش (Verification) حتماً باید در سمت سرور انجام شود تا از جعل رسید جلوگیری شود.

---

## 🚀 گام‌های مقیاس‌پذیری (برای ۱۰,۰۰۰+ کاربر)

### ۱. بهینه‌سازی SSR (Server Side Rendering)
- مهاجرت به **Next.js**. این کار باعث می‌شود سایت در گوگل (SEO) رتبه بگیرد و سرعت لود اولیه بسیار بالا برود.

### ۲. مدیریت صف (Queue System)
- برای درخواست‌های سنگین هوش مصنوعی (مثل تولید ویدیو Veo)، باید از سیستم صف (مانند BullMQ یا Redis) استفاده شود تا سرور کرش نکند.

### ۳. مانیتورینگ (Sentry)
- نصب ابزار Sentry برای ردیابی خطاهایی که کاربران با آن مواجه می‌شوند قبل از اینکه آنها گزارش دهند.

---

**تایید شده توسط:** تیم معماری فنی نخلستان معنا
